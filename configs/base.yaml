project:
  name: autoweld_vision
  version: 0.1.0

dataset:
  name: mvtec
  root: ./datasets/mvtec
  category: bottle
  image_size: [256, 256]

model:
  name: patchcore
  backbone: wide_resnet50_2
  layers: [layer2, layer3]

evaluation:
  metrics: [auroc, aupimo, fpr95]
  threshold: 0.65
